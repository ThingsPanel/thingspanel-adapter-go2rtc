# configs/config.yaml
server:
  port: 15001
  http_port: 15002
  heartbeatTimeout: 60 # 心跳超时时间(秒)

platform:
  url: "http://47.104.235.67:19999"
  mqtt_broker: "mqtt://47.104.235.67:11883"
  mqtt_username: "root"
  mqtt_password: "root"
  service_identifier: "OG-SZ501"  # 添加服务标识符
  template_secret: "e43823f2-7a91-133b-13d1-612df60fbfdc"  # 模板密钥，用于动态注册

log:
  level: "debug"
  filePath: "logs/app.log"
  enableFile: false  # 是否将日志输出到文件
  maxSize: 100
  maxBackups: 3
  maxAge: 28
  compress: true # 是否压缩日志文件
  
  # 设备独立日志配置
  device_log:
    enabled: true                    # 是否启用设备独立日志
    base_dir: "logs/devices"         # 设备日志基础目录
    max_size: 10                     # 单个设备日志文件最大大小(MB)
    max_age: 30                      # 设备日志文件保留天数
    compress: true                   # 是否压缩设备日志文件

# 协议配置 - 每个协议使用独立端口，目前该中间件只支持同时运行一个协议
protocols:
  sensor_protocol:
    enabled: true
    port: 15003
  
  gateway_protocol:
    enabled: false
    port: 15004
    
  simple_protocol:
    enabled: false
    port: 15005
    
  complex_protocol:
    enabled: false
    port: 15006
